# X API v2 Bearer Token from https://developer.x.com
# Pay-per-use plan is fine for this volume.
X_BEARER_TOKEN=

# Shared secret for incoming MCP requests. Generate one with:
#   openssl rand -hex 32
# This same value is used as the Bearer token in Claude's custom MCP connector.
MCP_AUTH_TOKEN=
